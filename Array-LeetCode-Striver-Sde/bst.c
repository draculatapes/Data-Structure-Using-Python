#include<stdio.h>
struct node
{
    struct node*lchild;
    struct node*rchild;
    int data;
};
tydef struct node Node;
void insertInBst(struct node*root):


int main()
{
   
   struct node root=(struct node*)malloc(sizeof(struct node))
   root.data=
   print("hello");
   return 0;
}